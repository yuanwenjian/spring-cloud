
spring:
    profiles:
        active: dev
    application:
        name: gateway
    cloud:
        config:
            uri: http://admin:admin@127.0.0.1:8761
            profile: dev
            name: gateway
